# Overnight Brief — 2026-05-29 — Quorum Calibrated-Auditable MAI-DxO

You are running an autonomous overnight session to maximally and HONESTLY improve the
Quorum benchmark. No human is available — make reasonable calls and continue. Pick up
exactly where the last session left off.

=== WHAT YOU ARE BUILDING ===
Quorum = the OPEN-SOURCE reproduction of Microsoft's MAI-DxO (arXiv 2506.22405): cost-
aware sequential diagnostic deliberation, MCP-native, MIT-licensed, on Claude Sonnet
4.6. Quorum's TWO structural differentiators over closed MAI-DxO are the whole point —
preserve and strengthen them, never trade them away:
  (1) HONEST CALIBRATED POSTERIORS — Brier + ECE on Hypothesis's per-diagnosis probs.
  (2) APPEND-ONLY AUDIT TRAILS — every agent message, Gatekeeper query, SafetyChecker
      verdict, per-turn JSONL.
The decided, current direction is the v2 "Calibrated-Auditable MAI-DxO" build on `main`
(tag v2.0). Earlier "v2 MedQA tier-ablation" and "v3 CAQ" labels are dead naming drift —
ignore them. This build IS the open-source MAI-DxO.

=== SYSTEM MAP ===
- Code: backend/src/quorum/ — orchestrator/panel.py (run_sequential), the 5 agents at
  orchestrator/agents/* (hypothesis/test_chooser/challenger/stewardship/checklist + base.py),
  gatekeeper/, audit/, calibration/, orchestrator/safety.py
- LLM client: backend/src/quorum/llm/client.py — complete() is the single API entry point.
- Tunable prompts: backend/src/quorum/orchestrator/prompts/*.md
- Arm A (full system): backend/config/panels/v2_quorum_calibrated.yaml (all 5 = Sonnet 4.6)
- Arm B (single-Sonnet floor): backend/config/panels/v2_single_sonnet.yaml
- Optional Opus mini-arm: backend/config/panels/v2_quorum_opus.yaml

=== AUTHORITATIVE DOCS (read in PHASE 0; if conflict, SPEC wins) ===
- Spec:  docs/superpowers/specs/2026-05-26-quorum-calibrated-auditable-mai-dxo-design.md
- Plan:  docs/superpowers/plans/2026-05-27-quorum-calibrated-mai-dxo.md
- Rules: .claude/rules/v2-benchmark.md  (HARD CONSTRAINTS)
- Last results + honest limitations: docs/results_v2.md

=== ONE DELIBERATE OVERRIDE OF THE RULES FILE (read this) ===
.claude/rules/v2-benchmark.md:16 says "EVAL runs once." TONIGHT you run the held-out
EVAL set TWICE on purpose: once for the clean baseline (PHASE 1, which was never validly
obtained — see below) and once as the FINAL number (end of PHASE 3). This is the only
sanctioned deviation, and it is safe ONLY because of the hardened anti-leakage firewall
below: the baseline EVAL is OBSERVED but its per-case content NEVER informs a single
tuning decision. If you cannot hold that firewall, do NOT run EVAL twice. Everything else
in the rules file is binding.

=== VERIFIED CURRENT STATE (confirmed against the repo 2026-05-29) ===
- Tree: clean, branch main, tag v2.0. Tests green. Tracked spend EXACTLY $15.01 of the
  $75 gate (data/results/.spend_total.txt).
- OpenRouter balance refilled to ~$80 — the credit cap that broke the last run is GONE.
- Arm B: CLEAN 30/30 baseline already on disk (data/results/v2-arm-b-final, 30 audit files)
  — 16.7% top-1, 46.7% top-1+partial. Reuse it; re-judge only if needed.
- Arm A: only 11/30 valid last run. 19 cases errored — the brief's prior session ATTRIBUTED
  this entirely to the old credit cap (all 8 MCR + all 5 RareBench APIStatusError + 6 NEJM
  "(no diagnosis)"). That attribution is PLAUSIBLE BUT UNVERIFIED — the perfect family-
  clustering (every MCR, every RareBench) is suspicious for a purely global credit cause.
  TREAT IT AS A HYPOTHESIS, NOT A FACT (see PHASE 1 canary + validity gate).
  On the 11 valid NEJM cases Arm A = 45.5% top-1 vs Arm B 18.2%.
- THERE IS NO TRUSTWORTHY 30-CASE ARM A NUMBER. Producing one is JOB #1.

=== ⚠️ IDEMPOTENT-SKIP GOTCHA (critical, verified in v2_runner.py:72-73,133-134) ===
The runner SKIPS any case whose <case_id>.audit.jsonl already exists in the run dir, and
the 19 errored cases already wrote error-stamped audit files in v2-arm-a-final. ALWAYS
use a FRESH --run-id for re-runs. Never re-point at v2-arm-a-final expecting retries —
it will skip the errors and falsely report "complete".

=== PHASE 0.5 — SESSION SETUP (do this FIRST, before any spend) ===
Run these once at session start so the $40 ceiling is enforced by the HOOK, not just your
own bookkeeping:
  1. export QUORUM_SPEND_HARD_STOP_USD=55     # 15.01 existing + ~40 incremental ≈ 55.
       # This LOWERS the gate (always allowed; the rule only forbids RAISING it). The
       # existing PreToolUse spend_gate.sh hook now hard-blocks any benchmark run at ~$55,
       # giving the $40 ceiling deterministic teeth even if your self-tracking drifts.
  2. DO NOT set QUORUM_DEFAULT_MAX_TOKENS. Credits are fine now, and capping output tokens
       (e.g. 3500) can TRUNCATE a long agent JSON response → JSONDecodeError — i.e. it would
       MANUFACTURE the exact failure-mode (c) you are trying to fix and confound PHASE 2/3.
       Leave the default 4096.
  3. Confirm: cat data/results/.spend_total.txt  →  must read ~15.01. If not, STOP and
       reconcile before spending.
  4. Note the per-run guard: v2_runner raises if projected cost > QUORUM_MAX_COST_USD
       (default $20). 30 Arm A cases × $0.50 = $15 < $20 so the EVAL commands pass, but pass
       --confirm-cost (already in the commands below) so a higher max-turns/n can't crash you.

=== REAL BENCHMARK COMMANDS (the plan's `quorum-eval --batch-api` strings are STALE) ===
Run the two EVAL commands in the BACKGROUND with stdout teed to a log, so a single stuck
case cannot silently consume the whole night and you have a morning-readable trace.

Arm A (EVAL):  cd backend && uv run python scripts/run_v2_benchmark.py --arm arm_a \
          --panel v2_quorum_calibrated --split eval --run-id <ID> --max-turns 12 \
          --confirm-cost 2>&1 | tee ../data/results/<ID>.log
Arm B (EVAL):  cd backend && uv run python scripts/run_v2_benchmark.py --arm arm_b \
          --panel v2_single_sonnet --split eval --run-id <ID> --confirm-cost
TUNE (tuning iterations only): same as Arm A, with --split tune (5 cases) --run-id <ID> \
          --max-turns 12 --confirm-cost
CANARY (PHASE 1, cheap, no leakage): cd backend && uv run python scripts/run_v2_benchmark.py \
          --arm arm_a --panel v2_quorum_calibrated --split tune -n 1 --max-turns 12 \
          --confirm-cost --run-id overnight-canary
Judge:  cd backend && uv run python scripts/judge_v2_run.py data/results/<ID> \
          --judge-model anthropic/claude-sonnet-4-6
Metrics:python3 backend/scripts/compute_v2_metrics.py data/results/<ID>
Summary:python3 backend/scripts/summarize_v2_run.py data/results/<ID>
Agent smoke (cheap tuning signal, ~$0.10): cd backend && uv run python scripts/prompt_iteration_eval.py
Results land in data/results/<ID>/{*.audit.jsonl, judge_results.json, manifest.json}.

=== BUDGET — $40 SELF-TRACKED CEILING (hook-enforced at ~$55 total) ===
Existing tracked total $15.01; hard gate $75 (do NOT raise; v2-benchmark rule). $40 is YOUR
incremental ceiling tonight, now ALSO enforced by QUORUM_SPEND_HARD_STOP_USD=55. Cheap-first.
Reserve ~$4 (10%) for report + final commit.
Cost guide: Arm A 30-case @ max-turns 12 ≈ $10-15 (~$0.30-0.60/case); Arm B 30-case ≈ $1.40
(reuse the clean one — likely $0); judge/arm ≈ $0.50; one TUNE pass (5 cases) ≈ $1.5-3;
canary (1 case) ≈ $0.50. Budget plan: PHASE 0.5+canary (~$1) + PHASE 1 baseline (~$13) +
tuning iterations (~$11) + final EVAL (~$13) ≈ $38. Opus mini-arm ONLY per the gate below.
LOG EVERY run immediately:  bash backend/scripts/log_spend.sh <amt> "<label>"
CHECK before each big run:   bash backend/scripts/spend_gate.sh
If a single Arm A case exceeds ~$0.60, drop --max-turns to 8 for the rest.

=== "BETTER" — PRIORITY-ORDERED ===
1. Arm A top-1 accuracy on the 30-case EVAL (headline).  2. Arm A top-1+partial.
3. Calibration: lower Brier/ECE on commit-time posterior, recomputed on VALID cases only
   (the 1.37/0.49 in results_v2.md are inflated by errored empties — fix this).
4. Audit completeness.  5. Cost/case (never trade >2pp accuracy for cost).

DEFINITION OF "VALID CASE" (use this everywhere — baseline count, validity gate, Brier/ECE):
a case is VALID iff its verdict is non-error AND it has a non-empty committed diagnosis AND
a numeric commit-time posterior. Errored / empty / "(no diagnosis)" cases are INVALID and
are EXCLUDED from accuracy and calibration denominators (report invalid count separately).

=== ANTI-LEAKAGE / ANTI-CHEATING (non-negotiable, HARDENED) ===
- The 30-case EVAL is HELD OUT. Run it at most TWICE tonight: once for the clean baseline
  (PHASE 1), once as the FINAL number (end of PHASE 3). NEVER tune by scoring on EVAL.
- FIREWALL (this is what makes two EVAL runs honest): after the PHASE 1 baseline you may
  read ONLY the aggregate validity count and the headline metrics (top-1, top-1+partial,
  Brier, ECE). You may NOT read or reason over per-case EVAL diagnoses, judge rationales,
  or transcripts to decide what to change. EVERY hypothesis and tuning decision comes from
  the 5 TUNE cases, the agent smoke harness, and the ERRORED audit logs — never from
  baseline EVAL case content. If you catch yourself inspecting a baseline EVAL case to
  motivate a change, STOP: that is leakage.
- ALL improvement iteration uses the 5 TUNE cases (--split tune) + the agent smoke harness
  ONLY. Per-change re-benchmark = TUNE, never EVAL.
- NEVER edit data/cases/eval_corpus_v2/splits.json (frozen).
- Forbidden "improvements": hardcoding/special-casing known cases, loosening the judge,
  weakening SafetyChecker to force commits, or anything that lifts the score without real
  diagnostic gain. If a change only helps by gaming the metric, REVERT it.

=== HARD CONSTRAINTS (.claude/rules/v2-benchmark.md) ===
No new agent classes (the 5 are the contract). No new runtime deps. Sonnet 4.6 for all 5
agents; Opus 4.7 ONLY in the Phase-8 mini-arm and ONLY if the Opus gate below is satisfied.
NEVER commit/redistribute data/cases/eval_corpus_v2/nejm_sample.json (paywalled, gitignored).
Don't touch v1 frozen panels. Git: commit locally per kept change; NEVER push, force,
reset --hard, checkout --, clean, or touch remote; no --no-verify. Leave tree clean.
OPUS GATE (reconciles rule #3 "≥$20 remaining" with budget "≥$8 unspent" — satisfy BOTH):
run the Opus mini-arm only if core work is done AND ≥$8 of your $40 incremental remains
AND total spend ≤ $55 (i.e. ≥$20 of the $75 gate remains).

=== WORKFLOW SKILLS (use these) ===
- PHASE 2 diagnosis: invoke superpowers:subagent-driven-development and dispatch PARALLEL
  READ-ONLY subagents (one per lens) — no paid LLM benchmark calls inside them, pure code/
  audit-trace reading. This is safe to parallelize.
- PHASE 3: any spend-incurring benchmark run stays SERIAL (single budget ledger). Use
  superpowers:systematic-debugging for failure-mode fixes and
  superpowers:verification-before-completion before each KEEP+COMMIT.

=== PHASES ===
PHASE 0 — ORIENT: read the 4 docs + git log -20 + this block. Do PHASE 0.5 session setup.
  Write ORIENTATION.md (done / missing / plan's next steps). No LLM spend.

PHASE 1 — CONFIRM BASELINE (with a cheap canary + a validity gate so you don't burn $13 on
  a doomed run):
  (1a) CANARY (~$0.50, NO leakage): run the CANARY command (1 TUNE case, full Arm A path).
       If it completes with a committed diagnosis, pipeline + credits are healthy end-to-end.
       If it errors, STOP and root-cause (PHASE 2 systematic-debugging) BEFORE the full run —
       the credit-cap story is then wrong and a full baseline would just reproduce garbage.
  (1b) FULL BASELINE: run the CLEAN full 30-case Arm A EVAL, FRESH run-id overnight-arm-a-30,
       --max-turns 12, in the background with the log tee. Judge + compute metrics. Reuse
       Arm B's clean v2-arm-b-final.
  (1c) VALIDITY GATE: count VALID cases (per the definition above). If < 27/30 valid (≥3
       errored/empty), this is NOT a trustworthy baseline — STOP all tuning, treat the
       failure cluster as JOB #1 (PHASE 2 + 3 fixes), and only re-run a full EVAL once fixed.
       Do NOT spend on prompt tuning while the baseline is degenerate.
  (1d) Recompute Brier/ECE on VALID cases ONLY. Record raw per-case in BASELINE.md (this is
       the real Arm A baseline that was missing). Log spend.

PHASE 2 — ADVERSARIAL READ-ONLY DIAGNOSIS (parallel subagents, six lenses): reduce layers /
  remove complexity / increase reliability / repo-policy violations / verified-vs-assumed /
  serves-original-intent. Inspect failed/errored audit JSONL traces. SEED HYPOTHESES TO
  VERIFY (all CONFIRMED live in main as of 2026-05-29 — premises re-checked against code):
    (a) temperature is declared in panel_config.py (Field default 0.0) and in EVERY v2 YAML
        (0.0 per agent), but LLMClient.complete() (client.py:164) takes NO temperature arg
        and never forwards it -> all 5 agents run at the API default temp, NOT deterministic
        0.0. Wire it through complete(). Reliability/reproducibility. (Highest leverage-per-
        dollar: near-zero cost, validated on TUNE + canary.) NOTE: the PHASE 1 baseline runs
        PRE-fix (nondeterministic, a single noisy draw); the final EVAL runs POST-fix
        (deterministic). State this in the report — the baseline→final delta partly reflects
        noise reduction, not only prompt gains.
    (b) panel.py _error_verdict (panel.py:795) returns total_cost_usd=0.0 -> errored cases
        under-count spend. Honest cost accounting.
    (c) StewardshipAgent JSONDecodeError seen killing cases (memory) + Gatekeeper substring-
        miss behavior -> JSON-parse robustness across agents (orchestrator/agents/*). Do NOT
        "fix" this by capping max_tokens — that causes truncation. Fix the parsing/retry.
  Emit BACKLOG.md ranked by ACCURACY-LEVERAGE-PER-DOLLAR: each item = hypothesis + file it
  touches + predicted test cost + the TUNE metric that confirms it.

PHASE 3 — EXECUTION LOOP (serial, budget-tracked): work down BACKLOG.md; ONE surgical
  change -> TUNE re-benchmark/agent smoke -> keep+commit-with-delta or revert; 3-iteration
  defer rule. Suggested first item: hypothesis (a) temperature wiring (cheap, high
  reliability lift) — re-run the canary after wiring to confirm determinism. When ~$4
  remains, STOP iterating and run the FINAL 30-case Arm A EVAL (fresh run-id
  overnight-arm-a-final) as the official number. OPTIONAL Opus mini-arm (10 cases,
  v2_quorum_opus) only if the Opus gate above is satisfied and core work is done.

MORNING REPORT -> REPORT.md: baseline vs final Arm A 30-case (Arm B + literature MAI-DxO
  85% as reference); per-change attribution w/ commit hashes + measured deltas; the
  baseline-was-nondeterministic caveat from hypothesis (a); resolved vs open themes;
  deferred items; remaining ranked hypotheses; total budget spent (from
  data/results/.spend_total.txt). Final clean commit. Tree clean.

ARTIFACT PATHS (create dir data/results/overnight-2026-05-29/):
  ORIENTATION.md, BASELINE.md, BACKLOG.md, LOG.md (append per action:
  what/cost/delta/keep|revert), REPORT.md
  EVAL run dirs: data/results/overnight-arm-a-30/, data/results/overnight-arm-a-final/
  Canary dir: data/results/overnight-canary/
