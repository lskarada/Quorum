# Overnight Brief — 2026-05-30 — Quorum Accuracy-Maximization Campaign (v2.1)

You are running an autonomous overnight session to maximally and HONESTLY raise Arm A's
full-credit top-1 accuracy on the frozen 30-case EVAL. No human is available — make
reasonable calls and continue. Pick up exactly where the 05-29 session left off.

=== WHAT YOU ARE BUILDING ===
Quorum = the OPEN-SOURCE reproduction of Microsoft's MAI-DxO (arXiv 2506.22405): cost-
aware sequential diagnostic deliberation, MCP-native, MIT-licensed, on Claude Sonnet 4.6.
Quorum's TWO structural differentiators over closed MAI-DxO are the whole point — preserve
and strengthen them, never trade them away:
  (1) HONEST CALIBRATED POSTERIORS — Brier + ECE on Hypothesis's per-diagnosis probs.
  (2) APPEND-ONLY AUDIT TRAILS — every agent message, Gatekeeper query, SafetyChecker
      verdict, per-turn JSONL.
Current direction is the v2 "Calibrated-Auditable MAI-DxO" build on `main` (tag v2.0).

=== THE GOAL (this campaign) ===
Push Arm A (the 5-agent calibrated panel, all Sonnet 4.6) FULL-CREDIT top-1 as high as
possible on the held-out 30-case EVAL, reported against Arm B (single Sonnet) on the same
set. Calibration (Brier/ECE) is a NON-REGRESSION GUARDRAIL, not the optimization target:
a change that lifts top-1 but worsens Brier/ECE beyond noise is REVERTED.

=== WHAT IS NEW vs THE 05-29 RUN ===
The 05-29 run could only tune against 5 TUNE cases + the agent-smoke harness — a thin
signal. This campaign adds a **DEV set of 26 cases** (held out from EVAL) so the P3
optimization loop has real statistical traction WITHOUT ever touching EVAL. The DEV set is
now BUILT (11 NEJM + 15 MCR; see DEV SET below) — that was the precondition blocking this
campaign, and it is satisfied.

=== SYSTEM MAP ===
- Code: backend/src/quorum/ — orchestrator/panel.py (run_sequential), the 5 agents at
  orchestrator/agents/* (hypothesis/test_chooser/challenger/stewardship/checklist + base.py),
  gatekeeper/, audit/, calibration/, orchestrator/safety.py
- LLM client: backend/src/quorum/llm/client.py — complete() is the single API entry point
  (temperature now wired through per-agent as of the 05-29 B3 fix).
- Tunable prompts: backend/src/quorum/orchestrator/prompts/*.md
- Arm A (full system): backend/config/panels/v2_quorum_calibrated.yaml (all 5 = Sonnet 4.6)
- Arm B (single-Sonnet floor): backend/config/panels/v2_single_sonnet.yaml

=== AUTHORITATIVE DOCS (read in PHASE 0; if conflict, SPEC wins) ===
- Spec:  docs/superpowers/specs/2026-05-26-quorum-calibrated-auditable-mai-dxo-design.md
- Plan:  docs/superpowers/plans/2026-05-27-quorum-calibrated-mai-dxo.md
- Rules: .claude/rules/v2-benchmark.md  (HARD CONSTRAINTS; budget now $142, see BUDGET below)
- 05-29 brief + its REPORT.md: docs/overnight/2026-05-29-brief.md and
  data/results/overnight-2026-05-29/REPORT.md (the most recent honest state).
- CAVEAT: docs/results_v2.md is dated 2026-05-27 and PRE-DATES the 05-29 overnight run; its
  headline numbers are the credit-cap-degraded ones. Treat the 05-29 REPORT.md + the run
  dirs (overnight-arm-a-30, overnight-arm-a-final) as the current truth, not results_v2.md.

=== DEV SET (the new tuning corpus — precondition now SATISFIED, n=26) ===
The DEV set is now 26 cases across TWO files. Both are disjoint from EVAL∪TUNE by construction.
- FILE 1 — data/cases/eval_corpus_v2/dev_nejm_sample.json  (11 NEJM CPC cases, rich schema:
  initial_presentation / available_findings[] / hidden_discussant_differential /
  ground_truth_*). GITIGNORED + PAYWALLED — never commit, redistribute, or publish its text.
- FILE 2 — data/cases/eval_corpus_v2/dev_mcr_sample.json  (15 MedCaseReasoning/PMC cases,
  lean schema: presentation / ground_truth_diagnosis / reasoning_trace). MIT-licensed, PUBLIC,
  COMMITTED (allowlisted in .gitignore). Minted by backend/scripts/build_dev_augment.py with a
  FIXED seed (20260530); re-running that script reproduces the exact 15 ids byte-for-byte.
- Both files are held SEPARATE from nejm_sample.json / mcr_sample.json BY DESIGN so a
  splits.json re-split can never pull DEV into EVAL/TUNE. Leakage re-asserted 2026-05-30:
  DEV ∩ (TUNE ∪ EVAL) = ∅ (all 26 ids disjoint from the 35 in splits.json); the MCR sampler
  excludes every id already in splits.json (tune+eval) AND every NEJM DEV id by construction;
  none of the 11 NEJM ground-truth disease names appear in any initial_presentation.
- NEJM DEV manifest (citation-level, safe to reference — NOT the paywalled body):
    nejm-cpc-25-2025  NEJMcpc2412535  Aortic stenosis with Heyde's syndrome            [medium]
    nejm-cpc-24-2025  NEJMcpc2312739  Lyme carditis                                    [medium]
    nejm-cpc-23-2025  NEJMcpc2309348  PVOD with pulmonary capillary hemangiomatosis    [hard]
    nejm-cpc-22-2025  NEJMcpc2412531  Anti-NMDA encephalitis + ovarian germ-cell tumor [hard]
    nejm-cpc-21-2025  NEJMcpc2412532  Nocardia farcinica + M. abscessus coinfection    [hard]
    nejm-cpc-20-2025  NEJMcpc2412527  S. aureus-infected carotid pseudoaneurysm        [hard]
    nejm-cpc-19-2025  NEJMcpc2412528  Powassan virus encephalitis                      [hard]
    nejm-cpc-18-2025  NEJMcpc2300897  Erdheim-Chester disease                          [hard]
    nejm-cpc-17-2025  NEJMcpc2412510  Donor-derived disseminated strongyloidiasis      [hard]
    nejm-cpc-16-2025  NEJMcpc2412524  Granulomatosis with polyangiitis                 [hard]
    nejm-cpc-15-2025  NEJMcpc2412526  Hantavirus cardiopulmonary syndrome              [medium]
- MCR DEV manifest (15 PMC ids, public): PMC10976528, PMC11106875, PMC11350371, PMC11437731,
  PMC3039604, PMC3921287, PMC4601964, PMC5094134, PMC5563901, PMC5582527, PMC6177055,
  PMC8222742, PMC8280531, PMC9062141, PMC9806393 (each prefixed mcr- in the file).
- SIZE: 26 DEV cases (11 NEJM + 15 MCR) = a 5.2x-richer tuning signal than the 5 TUNE cases.
  Augmentation is DONE; further RareBench pulls are OPTIONAL (needs a HuggingFace download —
  no local pool — so skip unless time/budget is ample). 26 is comfortably "robust enough."
- ⚠️ DEV PLUMBING IS NOT WIRED YET. run_v2_benchmark.py loads cases via splits.json
  (tune/eval only). Before you can run the DEV set you must add a dev source that reads
  case_ids from BOTH dev_nejm_sample.json AND dev_mcr_sample.json — WITHOUT adding DEV ids to
  splits.json (splits.json is FROZEN, never edit it). The two files have DIFFERENT schemas
  (NEJM has available_findings[] for progressive Gatekeeper reveal; MCR is flat presentation +
  diagnosis), so the dev loader must handle both: feed MCR's `presentation` as the initial
  context with no staged findings. Verify the loader path in scripts/run_v2_benchmark.py
  first; this is the first PHASE 1 engineering task.

=== VERIFIED CURRENT STATE (confirmed against the repo 2026-05-30) ===
- Tree: branch main, tag v2.0. Tracked spend = **$42.35** of the $142 gate
  (data/results/.spend_total.txt). The 05-29 run completed: clean 30-case Arm A baseline
  (overnight-arm-a-30), B1/B3/B4 + B2-hypothesis TUNE iterations, and a FINAL 30-case Arm A
  EVAL (overnight-arm-a-final) all landed. Read the 05-29 REPORT.md for the resulting numbers.
- Temperature is now wired per-agent (B3 fix) so Arm A is deterministic at the configured
  temp; errored-case cost accounting (B4) and JSON-parse robustness (B1) fixes also landed.

=== BUDGET — $142 HARD STOP (raised by explicit user approval 2026-05-30) ===
- The ceiling is now **$142** (spend_gate.sh hard stop $142, warn $135). This was raised from
  the prior $80/$75 by EXPLICIT user approval on 2026-05-30 for this campaign, and both
  .claude/rules/v2-benchmark.md:15 AND backend/scripts/spend_gate.sh are already updated to
  match. Tracked spend is **$42.35**, so ~**$99.65 of headroom remains** to the $142 stop.
- Do NOT raise QUORUM_SPEND_HARD_STOP_USD above $142 on your own — $142 is the approved cap;
  any further increase needs a fresh explicit user approval.
- Setup: export QUORUM_SPEND_HARD_STOP_USD=142 (do not exceed). Still cheap-first — headroom
  is for CONFIRMING good levers on full DEV, not for brute-forcing.
- Cost guide: a full DEV pass (26 cases, Arm A, max-turns 12) ≈ $9-15; full EVAL (30) ≈ $10-13;
  one TUNE pass (5) ≈ $1.5-3; agent smoke (prompt_iteration_eval.py) ≈ $0.10. With ~$100 of
  headroom a naive "full DEV re-run per lever" loop still burns out in ~6-8 levers, so SCREEN
  levers cheaply (agent smoke + the 5 TUNE cases) FIRST and spend a full DEV pass only to
  CONFIRM a lever that already looks good — reserve EVAL for the 2 sanctioned touches.
- LOG EVERY run immediately:  bash backend/scripts/log_spend.sh <amt> "<label>"
  CHECK before each big run:   bash backend/scripts/spend_gate.sh

=== ⚠️ IDEMPOTENT-SKIP GOTCHA (verified in v2_runner.py) ===
The runner SKIPS any case whose <case_id>.audit.jsonl already exists in the run dir. ALWAYS
use a FRESH --run-id for re-runs; never re-point at an existing dir expecting retries.

=== ANTI-LEAKAGE / ANTI-CHEATING (non-negotiable) ===
- The 30-case EVAL is HELD OUT. Run it at most TWICE this campaign: once for the baseline
  (PHASE 2), once as the FINAL number (end of PHASE 4). NEVER tune by scoring on EVAL.
- FIREWALL (what makes two EVAL touches honest): after the PHASE 2 EVAL baseline you may read
  ONLY the aggregate metrics (top-1, top-1+partial, Brier, ECE, valid count). You may NOT
  read or reason over per-case EVAL diagnoses, judge rationales, or transcripts to decide
  what to change. EVERY tuning decision comes from the DEV set, the 5 TUNE cases, and the
  agent-smoke harness — NEVER from EVAL case content. Inspecting an EVAL case to motivate a
  change IS leakage: stop.
- ALL improvement iteration uses DEV (+ TUNE + agent smoke). Per-change re-benchmark = DEV or
  TUNE, never EVAL.
- NEVER edit data/cases/eval_corpus_v2/splits.json (frozen). Never add DEV ids to it.
- Forbidden "improvements": hardcoding/special-casing known cases, loosening the judge,
  weakening SafetyChecker to force commits, capping max_tokens (truncates long agent JSON ->
  manufactured parse errors), or anything that lifts the score without real diagnostic gain.
  If a change only helps by gaming the metric, REVERT it.

=== HARD CONSTRAINTS (.claude/rules/v2-benchmark.md) ===
No new agent classes (the 5 are the contract). No new runtime deps. Sonnet 4.6 for ALL 5
agents — this campaign's HEADLINE STAYS SONNET-ONLY; do NOT add an Opus arm. Never
commit/redistribute the paywalled NEJM JSON (nejm_sample.json, dev_nejm_sample.json — both
gitignored). Don't touch v1 frozen panels. Git: commit LOCALLY per kept change; NEVER push,
force, reset --hard, checkout --, clean, or touch the remote; no --no-verify. Leave tree clean.

=== PHASES ===
PHASE 0 — ORIENT: read the authoritative docs + the 05-29 REPORT.md + git log -20 + this
  block. export QUORUM_SPEND_HARD_STOP_USD=142. Confirm cat data/results/.spend_total.txt
  reads ~42.35; if not, reconcile before spending. Write ORIENTATION.md. No LLM spend.

PHASE 1 — DEV SET READY:
  (1a) WIRE DEV PLUMBING (no spend): add a dev case-source that reads BOTH dev_nejm_sample.json
       AND dev_mcr_sample.json (do NOT edit splits.json). The two schemas differ (NEJM has
       available_findings[]; MCR is flat presentation+diagnosis) — handle both; feed MCR's
       `presentation` as initial context with no staged findings. Dry-run: all 26 DEV cases load.
  (1b) AUGMENT — ALREADY DONE (15 MCR cases minted by build_dev_augment.py, seed 20260530,
       committed as dev_mcr_sample.json). DEV is now 26. FURTHER augmentation is OPTIONAL and
       NOT needed: only pull seed-pinned RareBench (needs a HuggingFace download — no local
       pool) if you specifically want rare-disease diversity AND have ample time/budget;
       re-assert disjointness if you do. Otherwise skip — 26 is robust enough.
  (1c) Re-run the leakage assertion (DEV ∩ (TUNE∪EVAL) = ∅, all 26 ids) and record DEV size
       (26: 11 NEJM + 15 MCR) in BASELINE.md.

PHASE 2 — BASELINES (EVAL touch #1):
  (2a) Run Arm A on DEV (fresh run-id dev-baseline) -> judge -> metrics. This is your tuning
       reference; record per-case top-1/partial + Brier/ECE on VALID cases only.
  (2b) Run Arm A + Arm B on the 30-case EVAL (fresh run-ids) -> judge -> metrics. Record ONLY
       the aggregates (firewall). This is the official pre-optimization headline.
  (2c) VALIDITY GATE: a case is VALID iff non-error AND non-empty committed diagnosis AND
       numeric posterior. If < ~90% valid on DEV or EVAL, STOP tuning and fix the failure
       cluster first (systematic-debugging) — a degenerate baseline is not tunable.

PHASE 3 — OPTIMIZATION LOOP (serial, budget-tracked, DEV-only signal). Work a RANKED backlog;
  one surgical change -> cheap screen (agent smoke + 5 TUNE) -> if promising, CONFIRM on full
  DEV -> KEEP+commit-with-delta iff DEV top-1 ↑ AND no calibration regression beyond noise,
  else REVERT. 3-strike defer rule per lever. Candidate levers, ranked by expected
  accuracy-leverage-per-dollar (screen cheap ones first):
    1. Self-consistency / majority-vote over k Hypothesis samples (raise temp for diversity,
       vote the mode; keep the calibrated posterior from the aggregate).
    2. Explicit verification pass before commit (a "does the evidence actually support the
       top candidate?" check that can demote an overconfident lead).
    3. Stronger Hypothesis prompt (broaden the early differential; force consideration of the
       dual-diagnosis pattern — several DEV cases (20/21/22) are two-diagnosis cases).
    4. Smarter consensus + re-ranking at commit time (use Challenger/Stewardship signal to
       re-rank rather than only threshold the top posterior).
    5. TestChooser information-gain ordering (request the most discriminating finding first;
       fewer wasted turns -> more budget for deliberation within max-turns).
  Each backlog item = hypothesis + file it touches + predicted screen cost + the DEV metric
  that confirms it. When ~$4 remains under the gate, STOP iterating.

PHASE 4 — FINAL (EVAL touch #2): run the kept configuration on the 30-case EVAL (fresh
  run-id) as the OFFICIAL final number. Judge + metrics. git-tag the frozen config locally
  (e.g. v2.1-accuracy-final). Do NOT push.

PHASE 5 — REPORT -> REPORT.md: pre-optimization vs final Arm A 30-case EVAL (Arm B + the
  literature MAI-DxO ~85% as references); per-lever attribution with commit hashes + measured
  DEV deltas; calibration (Brier/ECE) non-regression check; kept vs reverted levers; deferred
  items; remaining ranked hypotheses; total spend (from .spend_total.txt). Final clean commit.

ARTIFACT PATHS (create dir data/results/overnight-2026-05-30/):
  ORIENTATION.md, BASELINE.md, BACKLOG.md, LOG.md (append per action:
  what/cost/delta/keep|revert), REPORT.md.
  Run dirs: data/results/dev-baseline/, data/results/eval-baseline-arm-{a,b}/,
  data/results/eval-final-arm-a/.
