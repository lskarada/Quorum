[project]
name = "quorum-workspace"
version = "0.1.0"
requires-python = ">=3.11,<3.13"

[tool.uv.workspace]
members = ["backend"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "RUF"]
